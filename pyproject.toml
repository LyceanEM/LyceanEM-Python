[build-system]
requires = ["setuptools>=42", "wheel", "scikit-build-core", "typing_extensions","nvidia-cuda-runtime-cu11"]
build-backend = "scikit_build_core.build"
[tool.setuptools_scm]
write_to = "lyceanem/_version.py"
[tool.scikit-build]
build-dir = "build"



